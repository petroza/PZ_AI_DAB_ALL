# Python / venv / cache
__pycache__/
*.pyc
.venv/
.venv_xtts/
work/

# Binárky, modely a hlasy (stahují se zvlášť — viz README)
*.gguf
*.onnx
*.onnx.json
*.exe
*.dll

# Běhová data (placeholdery .gitkeep zůstávají)
uploads/*
outputs/*
jobs/*
logs/*
models/*
tools/*
voices/*
!**/.gitkeep
# zdrojové skripty v tools/ (ne binárky) – ty chceme verzovat
!tools/xtts_server.py
!tools/dabtest.sh

# Tajný worker token – jen lokálně/na serveru, ne do gitu
web/config.php
worker_config.json
web/data/
